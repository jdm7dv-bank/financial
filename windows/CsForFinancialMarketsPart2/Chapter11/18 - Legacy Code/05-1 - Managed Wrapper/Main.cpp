// Main.cpp
//
// Contains only the include of the header files because the classes are implemented in the header file.
// If the classes were implemented in a separate file, this was not necessary.

#include "ManagedWrapper.hpp"
